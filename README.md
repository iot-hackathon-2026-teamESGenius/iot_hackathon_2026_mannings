# IOT Hackathon 2026 - Mannings Store Pickup SLA Optimization

<p align="center">
  <strong>门店自提SLA优化系统</strong><br>
  DFI Retail Group - Mannings (万宁)
</p>

---

## 🎯 项目概述

为DFI Retail Group - Mannings开发端到端的**门店取货SLA优化系统**，通过AI/ML预测和数学优化算法提升顾客取货体验并控制总成本。

### 核心挑战

| 挑战 | 描述 | 解决方案 |
|-----|------|---------|
| A. 需求预测波动 | 促销/长尾SKU/门店差异/周末高峰/季节性 | Prophet + 外部因素融合 |
| B. 库存不确定性 | 账面库存 ≠ 实际可履约库存 | 动态安全库存优化 |
| C. 补货计划 | 交期不确定性，需韧性方案 | 鲁棒优化算法 |
| D. 车队调度 | 直接影响Pickup SLA | CVRPTW + 多情景优化 |
| E. 门店处理时间 | 缺乏可信Pickup Promise | 概率SLA预测 |

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         前端层 (Vue3 + uniapp)                           │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐   │
│  │ 登录    │ │ 首页    │ │ 预测    │ │ 决策    │ │ SLA预警        │   │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘ └───────┬─────────┘   │
└───────┼──────────┼──────────┼──────────┼───────────────┼───────────────┘
        │          │          │          │               │
        ▼          ▼          ▼          ▼               ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      REST API 层 (FastAPI)                               │
│  /api/auth  /api/dashboard  /api/forecast  /api/planning  /api/sla      │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
┌─────────────────────────────────┼───────────────────────────────────────┐
│                        核心模块层                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │
│  │IDataFetcher  │  │IDemand       │  │IRouting      │  │ISLA         │  │
│  │数据获取      │  │Forecaster    │  │Optimizer     │  │Predictor    │  │
│  │              │  │需求预测      │  │路径优化      │  │SLA预测      │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  └─────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────────┐   │
│  │IDistance     │  │IInventory    │  │RobustRoutingOptimizer       │   │
│  │Calculator    │  │Optimizer     │  │鲁棒路径优化 (创新点)         │   │
│  │距离计算      │  │库存优化      │  │                              │   │
│  └──────────────┘  └──────────────┘  └──────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼ (Stage 2)
┌─────────────────────────────────────────────────────────────────────────┐
│                      智能体层 (预留接口)                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │
│  │IDemand       │  │IInventory    │  │IRouting      │  │ISLAAlert    │  │
│  │ForecastAgent │  │OptAgent      │  │DispatchAgent │  │Agent        │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  └─────────────┘  │
│                          ▲                                              │
│                          │                                              │
│                 IMultiAgentCoordinator                                  │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📁 项目结构

```
iot_hackathon_2026_mannings/
├── config/                          # 配置文件
│   ├── modules.yaml                 # 模块配置
│   ├── agents.yaml                  # 智能体配置 (Stage 2)
│   └── pipelines/                   # 流水线配置
├── src/
│   ├── core/                        # 核心框架
│   │   ├── interfaces.py            # 接口定义
│   │   ├── module_registry.py       # 模块注册
│   │   └── orchestrator.py          # 编排器
│   ├── modules/                     # 功能模块
│   │   ├── data/                    # 数据获取
│   │   ├── distance/                # 距离计算
│   │   ├── forecasting/             # 需求预测
│   │   ├── routing/                 # 路径优化 ⭐
│   │   │   └── implementations/
│   │   │       ├── ortools_optimizer.py      # Baseline CVRPTW
│   │   │       ├── robust_optimizer.py       # 鲁棒优化器 (创新)
│   │   │       └── scenario_generator.py     # 情景生成器
│   │   ├── inventory/               # 库存优化
│   │   ├── sla/                     # SLA预测
│   │   └── visualization/           # 可视化
│   ├── api/                         # REST API服务
│   │   ├── main.py                  # FastAPI入口
│   │   └── routers/                 # API路由
│   │       ├── auth.py              # 认证服务
│   │       ├── forecast.py          # 预测服务
│   │       ├── planning.py          # 决策规划
│   │       ├── sla.py               # SLA服务
│   │       └── dashboard.py         # 数据看板
│   ├── agents/                      # 智能体接口 (Stage 2预留)
│   │   ├── interfaces.py            # Agent接口定义
│   │   └── __init__.py
│   └── main.py                      # 主入口
├── data/                            # 数据目录
├── tests/                           # 测试用例
├── scripts/                         # 脚本工具
├── docs/                            # 文档
├── requirements.txt                 # Python依赖
└── environment.yml                  # Conda环境
```

---

## 🚀 快速开始

### 环境要求

- **Python**: 3.9 (严格要求)
- **包管理**: Miniconda (推荐)
- **操作系统**: Linux / macOS / Windows

### 方法一：Conda环境 (推荐)

```bash
# 1. 克隆仓库
git clone https://github.com/[组织名]/iot-hackathon-2026-mannings.git
cd iot-hackathon-2026-mannings

# 2. 使用脚本创建环境
bash scripts/setup_conda_environment.sh

# 3. 激活环境
conda activate mannings-sla

# 4. 验证环境
python scripts/verify_environment.py
```

### 方法二：手动安装

```bash
# 1. 创建Conda环境
conda env create -f environment.yml
conda activate mannings-sla

# 2. 或使用pip
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 运行服务

```bash
# 启动REST API服务 (供前端调用)
uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --reload

# 启动Streamlit Dashboard (内部调试)
streamlit run src/visualization/dashboard/app.py

# 运行路径优化Demo
python -m src.modules.routing.implementations.demo
```

---

## 🔌 API接口

### 基础信息

| 属性 | 值 |
|-----|---|
| Base URL | `http://localhost:8000/api` |
| 文档 | `http://localhost:8000/docs` |
| 认证方式 | Bearer Token |

### 接口清单

| 模块 | 端点 | 方法 | 说明 |
|-----|------|-----|------|
| 认证 | `/auth/login` | POST | 用户登录 |
| 认证 | `/auth/validate` | GET | 验证Token |
| 看板 | `/dashboard/kpi` | GET | KPI指标 |
| 看板 | `/dashboard/trend` | GET | 趋势数据 |
| 预测 | `/forecast/demand` | GET | 需求预测 |
| 预测 | `/forecast/inventory` | GET | 库存展望 |
| 规划 | `/planning/replenishment` | GET | 补货计划 |
| 规划 | `/planning/schedules` | GET | 调度计划 |
| 规划 | `/planning/routes/optimize` | POST | 路径优化 |
| SLA | `/sla/orders` | GET | 订单列表 |
| SLA | `/sla/alerts` | GET | 预警列表 |
| SLA | `/sla/bottleneck` | GET | 瓶颈分析 |

---

## 🎨 前端集成

### 技术栈

- **框架**: Vue 3 (Options API) + uniapp
- **UI组件**: uni-ui
- **图表**: uCharts
- **后端通信**: uniCloud云函数 + REST API

### 品牌规范

| 属性 | 值 |
|-----|---|
| 主色调 | `#0066CC` (万宁品牌蓝) |
| 字体 | 无衬线体 (微软雅黑/Arial) |
| 一级标题 | 18px |
| 正文 | 14px |

### 风险等级颜色

| 等级 | 颜色 | 说明 |
|-----|------|------|
| 低 | `#0066CC` | 蓝色 |
| 中 | `#FFC107` | 黄色 |
| 高 | `#FD7E14` | 橙色 |
| 严重 | `#DC3545` | 红色闪烁+提示音 |

---

## 🧠 核心算法

### 路径优化 - 鲁棒优化器

基于 **Learning Enhanced Robust Routing** 方案，实现Scenario-based Robust Optimization：

```python
from src.modules.routing.implementations.robust_optimizer import RobustRoutingOptimizer
from src.modules.routing.implementations.scenario_generator import ScenarioGenerator

# 创建优化器
optimizer = RobustRoutingOptimizer(
    scenario_generator=ScenarioGenerator(num_scenarios=5),
    selection_criterion="min_max_distance"
)

# 从预测结果进行鲁棒优化
result = optimizer.robust_optimization_from_forecasts(
    stores=store_list,
    forecasts=demand_forecasts,
    vehicles=vehicle_fleet,
    distance_calculator=calculator,
    use_confidence_bounds=True
)
```

### 创新点

1. **多情景需求建模**: 从预测置信区间生成离散需求情景
2. **Min-Max鲁棒策略**: 优化最坏情景下的表现
3. **预测→决策闭环**: 预测不确定性直接驱动调度决策

---

## 🤖 智能体系统 (Stage 2)

### 预留接口

第二阶段将引入AI Agent增强系统智能化能力：

| Agent | 功能 | 状态 |
|-------|------|------|
| `IDemandForecastAgent` | 自主模型选择、外部因素融合 | 接口已定义 |
| `IInventoryOptimizationAgent` | 动态安全库存、强化学习优化 | 接口已定义 |
| `IRoutingDispatchAgent` | 实时路径重规划、多目标优化 | 接口已定义 |
| `ISLAAlertAgent` | 动态阈值、根因分析、自动缓解 | 接口已定义 |
| `IMultiAgentCoordinator` | 多Agent协同决策 | 接口已定义 |

### 启用方式

```yaml
# config/modules.yaml
agents:
  enabled: true  # Stage 2启用
```

---

## 👥 团队协作

### Git分支策略

| 分支 | 用途 |
|-----|------|
| `main` | 生产就绪代码 |
| `develop` | 开发集成分支 |
| `feature/frontend-*` | 前端功能开发 |
| `feature/data-*` | 数据工程开发 |
| `feature/routing-*` | 路径优化开发 |
| `feature/agent-*` | 智能体开发 (Stage 2) |

### Commit规范

```
feat(模块): 简短描述

# 示例
feat(routing): add robust optimizer with scenario-based optimization
feat(api): implement SLA alert endpoints
feat(frontend): add login page with authentication
```

### 测试账号

| 角色 | 账号 | 密码 |
|-----|------|------|
| 门店库存管理 | `store_manager` | `store123` |
| 物流配送团队 | `logistics_admin` | `logistics123` |
| 系统管理员 | `admin` | `admin123` |

---

## 📊 项目进度

### Stage 1 (当前)

- [x] 核心模块框架搭建
- [x] 需求预测模块 (Prophet)
- [x] 路径优化模块 (OR-Tools + 鲁棒优化)
- [x] SLA预测模块
- [x] REST API服务层
- [x] Streamlit可视化看板

### Stage 2 (计划)

- [ ] 智能体系统实现
- [ ] 强化学习集成
- [ ] 实时数据对接
- [ ] 生产环境部署

---

## 📚 文档

- [开发指南](docs/DEVELOPMENT_GUIDE.md)
- [Conda环境指南](docs/CONDA_ENVIRONMENT_GUIDE.md)
- [API接口文档](docs/api/module_interfaces.md)

---

## 📄 License

Copyright © 2026 DFI Retail Group. All rights reserved.
